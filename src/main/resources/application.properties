spring.application.name=CodeVerse

server.port=${PORT:9999}

#jsp 
spring.mvc.view.suffix=.jsp
spring.mvc.view.prefix=/WEB-INF/views/

#database
spring.datasource.url=${mysql://root:ELXGhWkTxszpciMWtvhEvBswGhudOnVw@nozomi.proxy.rlwy.net:47515/railway}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:ELXGhWkTxszpciMWtvhEvBswGhudOnVw}

#hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#mail configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:shivpatel5120@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wrgxmlmktyzgibgv}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

#max request data count 
server.tomcat.max-part-count=30
# Maximum size for a single file (e.g., 10MB)
spring.servlet.multipart.max-file-size=10MB
# Maximum size for the entire request (e.g., 12MB)
spring.servlet.multipart.max-request-size=12MB
#Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_Se2qvWAGXKiEEF}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:uDPOUt4opu8No0avZVbZuGi8}
