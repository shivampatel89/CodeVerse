server.port=${PORT:9999}

#database
spring.datasource.url=jdbc:mysql://localhost:3306/codeverse1
spring.datasource.username=root
spring.datasource.password=root

#hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#mail configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:shivpatel5120@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wrgxmlmktyzgibgv}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Mail toggles
app.mail.welcome.enabled=${MAIL_WELCOME_ENABLED:true}
app.mail.otp.enabled=${MAIL_OTP_ENABLED:true}

# Public app URL used in mail templates
app.base-url=${APP_BASE_URL:http://localhost:9999}

#Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_Se2qvWAGXKiEEF}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:uDPOUt4opu8No0avZVbZuGi8}